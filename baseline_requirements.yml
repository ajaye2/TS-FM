name: baseline
channels:
  - defaults
dependencies:
  - python=3.8
  - numpy
  - pandas
  - scikit-learn
  - pytorch
  - torchvision
  - pip
  - pip:
    - torchmetrics
    - transformers


# conda env create -f baseline_requirements.yml
# conda activate baseline
# conda remove -n baseline --all