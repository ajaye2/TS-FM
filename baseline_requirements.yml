name: baseline
channels:
  - defaults
dependencies:
  - python=3.8
  - numpy
  - pandas
  - scikit-learn
  - pytorch
  - torchvision
  - pip
  - ipykernel
  - pip:
    - torchmetrics
    - transformers
    - accelerate
    - matplotlib
    - torchinfo
    - einops
    - reformer_pytorch
    - sympy
    - PyYAML


# conda env create -f baseline_requirements.yml
# conda activate baseline
# conda deactivate 


# conda remove -n baseline --all